<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - TeleConnect</title>
    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
    <div class="login-container">
        <div class="login-form">
            <h1>Welcome to TeleConnect</h1>
            <p class="text-center mb-3">Manage your telecom services with ease</p>
            
            <% if (error) { %>
                <div class="alert alert-error">
                    <%= error %>
                </div>
            <% } %>
            
            <form method="POST" action="/login">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" required>
                </div>
                
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                
                <button type="submit" class="btn">
                    Login
                </button>
            </form>
            
            <div class="mt-3 text-center">
                <p>Demo Accounts:</p>
                <div class="demo-accounts">
                    <small>
                        <strong onclick="fillLogin('john.doe', 'password123')">john.doe</strong> / password123 (Has active plan)<br>
                        <strong onclick="fillLogin('jane.smith', 'password456')">jane.smith</strong> / password456 (New user)<br>
                        <strong onclick="fillLogin('test.user', 'test123')">test.user</strong> / test123 (Expiring plan)
                    </small>
                </div>
            </div>
        </div>
    </div>

    <script>
        function fillLogin(username, password) {
            document.getElementById('username').value = username;
            document.getElementById('password').value = password;
        }
    </script>
</body>
</html>
